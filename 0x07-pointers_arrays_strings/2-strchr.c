#include "main.h"

/**
 * _strchr - locates a character in a string
 * @s: string to check
 * @c: character to check for
 *
 * Return: pointer to check s with c or null
 */

char *_strchr(char *s, char c)
{
	int i;

	for (i = o; *(s + i); i++)
	{
		if (*(s(s + i) == c)
			return (s + i);
	}
	if (*(s + i) == c)
		return (s + i);
	return (0);
}
